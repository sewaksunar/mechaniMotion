
## to create new file with Manim
```
manim init project my-project --default // in case of here manim init project motion --default
```

## rendering first manim scene ( i.e. CreateCircle)
```
manim -pql main.py CreateCircle // for hight quality rendering qh
```
##

# 1 Activate your venv (if not already)
& C:/Users/sewak/Documents/Animation/mechaniMotion/motions/.venv/Scripts/Activate.ps1

# 2 (Optional) Install/upgrade Manim
pip install -U manim

# 3 Run your scene with preview, low quality (pql)
manim -pql mechanism.py MechanismAnimation